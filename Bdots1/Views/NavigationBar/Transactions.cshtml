@model IEnumerable<Bdots1.Payment>
@{
    ViewBag.Title = "Transactions";
}

<h2>My transactions</h2>

<div class="row">
    <div class="col-md-3">
        <p><b>DATE</b></p>
    </div>

    <div class="col-md-3">
        <p><b>PAYER</b></p>
    </div>

    <div class="col-md-3">
        <p><b>RECEIVER</b></p>
    </div>

    <div class="col-md-3">
        <p><b>PAYMENT</b></p>
    </div>
</div>

@foreach (var item in Model)
{
    <div class="row">
        <div class="col-md-3">
            @item.paymentDatetime
       </div>

        <div class="col-md-3">
            @item.Payers.username
        </div>

        <div class="col-md-3">
            @item.Receivers.username
        </div>

        <div class="col-md-3">
            @item.paymentSum <b>BDoken</b>
        </div>
    </div>
}